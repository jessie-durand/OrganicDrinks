{% extends 'base.html.twig' %}

{% block title %}CategoryDrink index{% endblock %}

{% block body %}
    <h1>CategoryDrink index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_drink in category_drinks %}
            <tr>
                <td>{{ category_drink.id }}</td>
                <td>{{ category_drink.name }}</td>
                <td>
                    <a href="{{ path('category_drink_show', {'id': category_drink.id}) }}">show</a>
                    <a href="{{ path('category_drink_edit', {'id': category_drink.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('category_drink_new') }}">Create new</a>
{% endblock %}
