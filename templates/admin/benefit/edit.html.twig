{% extends 'base.html.twig' %}

{% block title %}Modifier {{ benefit.name }}{% endblock %}

{% block body %}
 {% if is_granted("ROLE_ADMIN") %}
    <h1>Modifier {{ benefit.name }}</h1>

    {{ include('admin/benefit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('benefit_index') }}">Retour</a>

    {{ include('admin/benefit/_delete_form.html.twig') }}
{% else %}
<p> Vous devez être administrateur pour voir ce contenu ! </p>
{% endif %}
{% endblock %}
