{% extends 'base.html.twig' %}

{% block title %}Benefit{% endblock %}

{% block body %}
    <h1>Benefit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ benefit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ benefit.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ benefit.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ benefit.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('benefit_index') }}">back to list</a>

    <a href="{{ path('benefit_edit', {'id': benefit.id}) }}">edit</a>

    {{ include('admin/benefit/_delete_form.html.twig') }}
{% endblock %}
