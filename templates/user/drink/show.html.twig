{% extends 'base.html.twig' %}

{% block title %}Boisson {{ drink.name }}{% endblock %}

{% block body %}
{% include "user/userBase.html.twig" %}
    <h1>{{ drink.name }}</h1>
        <div class="ingredientShow">
            <div class="showBody">

            <img src="{{drink.image}}" alt="{{ drink.name }} poster">
                <div class="text">
                    <p>{{ drink.description }}</p>
                </div>
                <div class="liens">
                    <p> Les ingrédients :</p>
                        {% for ingredient in drink.ingredients %}
                            <li>
                                <a href="{{ path('ingredient_show', {'id' : ingredient.id}) }}">
                                {{ingredient.name}}
                                </a>
                            </li>
                        {% endfor %}
                </div>
            </div>
        </div>

        <center>
            <div class="btn">
                <a href="{{ path('ingredient_index') }}">
                    Retour
                </a>
            </div>
        </center>
</div>
{% include "user/userFooter.html.twig" %}
{% endblock %}
