{% extends 'base.html.twig' %}

{% block title %}CategoryDrink{% endblock %}

{% block body %}
    <h1>CategoryDrink</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_drink.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_drink.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_drink_index') }}">back to list</a>

    <a href="{{ path('category_drink_edit', {'id': category_drink.id}) }}">edit</a>

    {{ include('category_drink/_delete_form.html.twig') }}
{% endblock %}
