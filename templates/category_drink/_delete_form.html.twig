<form method="post" action="{{ path('category_drink_delete', {'id': category_drink.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_drink.id) }}">
    <button class="btn">Delete</button>
</form>
